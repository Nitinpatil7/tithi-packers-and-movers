# Backend origin only. Frontend API modules append /api themselves.
NEXT_PUBLIC_API_URL=https://your-backend-domain.com

# Optional. Enable Maps JavaScript, Places, Geocoding, and Directions APIs.
NEXT_PUBLIC_GOOGLE_MAPS_KEY=

# Temporary default is local OTP 123456. Set true after SMS/OTP credentials are ready.
NEXT_PUBLIC_USE_REAL_OTP=false

# Keep false in production. This project is wired to live backend APIs.
NEXT_PUBLIC_USE_DUMMY=false
