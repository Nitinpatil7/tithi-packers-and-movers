import FeedbackTokenClient from './FeedbackTokenClient';

export const metadata = {
  title: 'Submit Moving Feedback in Surat',
  description: 'Submit verified feedback for your Tithi Packers & Movers booking with No. 1 Packers and Movers in Surat.',
  alternates: { canonical: '/feedback' },
};

export default function FeedbackTokenPage() {
  return <FeedbackTokenClient />;
}
