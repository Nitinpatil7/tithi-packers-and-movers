import OrdinaryServiceClient from './OrdinaryServiceClient';

export const metadata = {
  title: 'Packing and Loading Services in Surat',
  description: 'Choose No. 1 Packers and Movers in Surat for packing, unpacking, loading and unloading services tailored to your move.',
  alternates: { canonical: '/book/ordinary-service' },
};

export default function OrdinaryServicePage() {
  return <OrdinaryServiceClient />;
}
