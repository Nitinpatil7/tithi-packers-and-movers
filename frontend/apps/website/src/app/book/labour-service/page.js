import LabourServiceClient from './LabourServiceClient';

export const metadata = {
  title: 'Labour and Vehicle Service in Surat',
  description: 'Hire No. 1 Packers and Movers in Surat for labour and vehicle service, including loading, unloading and local transport help.',
  alternates: { canonical: '/book/labour-service' },
};

export default function LabourServicePage() {
  return <LabourServiceClient />;
}
