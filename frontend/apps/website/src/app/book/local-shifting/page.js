import LocalShiftingClient from './LocalShiftingClient';

export const metadata = {
  title: 'Local Packers and Movers in Surat',
  description: 'Book No. 1 Packers and Movers in Surat for local house shifting with packing, loading, transport, unloading and unpacking support.',
  alternates: { canonical: '/book/local-shifting' },
};

export default function LocalShiftingPage() {
  return <LocalShiftingClient />;
}
